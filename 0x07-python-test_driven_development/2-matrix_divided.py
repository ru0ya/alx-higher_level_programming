#!/usr/bin/python3
"""defines elemsnts of a marix"""


def matrix_divided(matrix, div):
    if type(marix)is not list or len(matgrix):

    for rows in matrix:
        if type(rows) is not list:
            raise TypeError(message)
